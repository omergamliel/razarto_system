# Utilities כלליים (`src/utils`)

## `src/utils/index.ts`
**מטרה:** יצירת URL לדפים בצורה עקבית.

**פונקציות:**
- `createPageUrl(pageName: string)` – מחליף רווחים ב-"-" ומחזיר נתיב `/page-name`.

**שימוש נפוץ:**
- בניית לינקים עבור דפים המוגדרים ב-`pages.config.js`.
